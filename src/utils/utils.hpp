#ifndef UTILS_HPP
#define UTILS_HPP
#include <QtWidgets/QWidget>

namespace Reaction::Utils {
    class Geometry {
        public:
            void moveCenter(QWidget *window);
    };
}

#endif // UTILS_HPP
